var storedUsername = "myusername"; 
var storedPassword = "mypassword"; 

var inputUsername = "myusername"; 
var inputPassword = "mypassword"; 

if (inputUsername == storedUsername && inputPassword == storedPassword) {
  console.log("User can login");
} else {
  console.log("User cannot login");
}